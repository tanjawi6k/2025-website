import React from 'react';
export type Theme = 'dark' | 'light';
export type ThemeOptions = {
    theme?: Theme;
    save?: boolean;
};
type ThemeContext = {
    theme: Theme;
    setTheme: (theme: Theme) => void;
    options: ThemeOptions;
};
export declare const ThemeContext: React.Context<ThemeContext>;
export {};
